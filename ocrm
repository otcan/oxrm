#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

echo "Deprecated: ./ocrm has been renamed to ./oxrm. This compatibility wrapper will be removed in a future release." >&2
exec "$repo_root/oxrm" "$@"
